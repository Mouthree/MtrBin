//Todo:建一个part的结构体,该结构体存放名字和标签,标签分为有值和无值标签,输入的标签进行一个判定,有一个单独的库存放,添加查看标签指令,可以查看有值还是无值;添加标签查找,可以查找指定的值


pub fn add_part_in(name: &str, part: &str, num: &str) {

}

pub fn add_part_in_with_comment(name: &str, part: &str, num: &str, tag: &[&str]) {
    println!("name:{}", name);
    println!("part:{}", part);
    println!("tag:{:?}", tag);
}

pub fn add_tag_for() {
    
}